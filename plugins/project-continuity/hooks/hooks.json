{
  "description": "Map the exact bare $project-continuity plugin invocation to a one-shot log-session-summary skill call, and block other model-initiated log-session-summary skill calls. User slash-command invocations remain allowed.",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 hooks/block-bare-invocation.py"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Skill",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/block-model-skill.sh log-session-summary"
          }
        ]
      }
    ]
  }
}
